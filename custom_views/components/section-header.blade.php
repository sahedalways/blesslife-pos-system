<div class="ds-section-header-wrapper">
    @if ($subtitle)
        <span class="ds-subtitle">{{ $subtitle }}</span>
    @endif

    <h2 class="ds-page-title">{{ $title }}</h2>

    <div class="ds-title-bar"></div>
</div>
